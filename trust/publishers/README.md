# Trusted Publishers

Publisher records live here once accepted through governance review.

Each publisher should have:

- a stable publisher ID,
- an RSA public key,
- a contact path,
- an incident contact,
- any scope limits or quality restrictions.

The active machine-readable trust policy is `../trust.json`.
